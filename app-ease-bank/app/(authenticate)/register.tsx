import { AuthenticateForm } from '@/(components)'
import React from 'react'
import { StyleSheet } from 'react-native'

const register = () => {
    return (
        <AuthenticateForm type='register' />
    )
}

export default register

const styles = StyleSheet.create({})